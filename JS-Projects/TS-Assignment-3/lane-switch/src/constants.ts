type dimension = {
  CANVAS_HEIGHT: number;
  CANVAS_WIDTH: number;
};

export const DIMENSIONS: dimension = {
  CANVAS_HEIGHT: 600,
  CANVAS_WIDTH: 600,
};

type car = {
  CAR_WIDTH: number;
  CAR_HEIGHT: number;
};

export const CAR_DIMENSIONS: car = {
  CAR_WIDTH: 120,
  CAR_HEIGHT: 100,
};
