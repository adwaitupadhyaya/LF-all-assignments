type dimension = {
  CANVAS_HEIGHT: number;
  CANVAS_WIDTH: number;
};

export const DIMENSIONS: dimension = {
  CANVAS_HEIGHT: 600,
  CANVAS_WIDTH: 600,
};

type car = {
  CAR_WIDTH: number;
  CAR_HEIGHT: number;
};

export const CAR_DIMENSIONS: car = {
  CAR_WIDTH: 120,
  CAR_HEIGHT: 100,
};

type lane = {
  LANE_SPEED: number;
  LANE_HEIGHT: number;
  LANE_WIDTH: number;
};

export const LANE_DIMENSIONS: lane = {
  LANE_HEIGHT: 120,
  LANE_WIDTH: 20,
  LANE_SPEED: 5,
};
